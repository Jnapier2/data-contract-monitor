{
  "name": "data-contract-monitor-dashboard",
  "private": true,
  "version": "0.3.4",
  "description": "Framework-free TypeScript reviewer dashboard for Data Contract Monitor.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
